# Redis configuration reference for production deployment.
# The docker-compose.yml passes the equivalent flags directly on the
# `command:` line for local dev; use this file with `--include` for a
# real deployment.

maxmemory 256mb
maxmemory-policy allkeys-lru
appendonly yes
appendfsync everysec
timeout 300
tcp-keepalive 60
